//Use Array.from() to create a new array where each element is the square of the corresponding element in the original array.
const arr=[4,5,6,7];
const squre=Array.from(arr,(a)=>a*a);
console.log(squre);