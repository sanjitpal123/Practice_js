//Create an arrow function printMessage that takes a message as an argument and logs it. Use the call method to invoke printMessage with a specified context.
 function printMessage(message)
 {
  console.log(message);
 }
 printMessage.call(null,"hello sanjit");
